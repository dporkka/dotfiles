Convert this JavaScript code to TypeScript with strict types. Add interfaces and types
for all data structures, function signatures, and return values. Use strict mode
(noImplicitAny, strictNullChecks). Preserve all runtime behavior — the output should
be drop-in compatible apart from the type annotations.
